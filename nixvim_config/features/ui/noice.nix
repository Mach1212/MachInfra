{
  plugins.noice = {
    enable = true;
    settings = {
      # TODO: look into this messages ui
      # messages = true;
      presets = {
        # TODO: make the palette blocky like everything else
        command_palette = true;
      };
    };
  };
}
